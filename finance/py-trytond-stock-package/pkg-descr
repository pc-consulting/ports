The stock package module allows to store packaging information about
customer and supplier return shipments. A package groups together
some moves from the same shipment. The packages can be organized in a
tree structure. If a package is defined on a shipment, the
could be set to Done only if all moves are packaged.
