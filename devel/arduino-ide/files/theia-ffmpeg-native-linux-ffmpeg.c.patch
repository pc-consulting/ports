--- node_modules/@theia/ffmpeg/native/linux-ffmpeg.c.orig
+++ node_modules/@theia/ffmpeg/native/linux-ffmpeg.c
@@ -24,6 +24,7 @@
 
 char *load_ffmpeg_library(struct FFMPEG_Library *library, char *library_path)
 {
+    dlerror(); /* clear stale loader error */
     void *handle = dlopen(library_path, RTLD_NOW);
     char *error = dlerror();
     if (error != NULL)
